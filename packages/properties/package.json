{
  "name": "@accelssa/properties",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "AccelSSA Category 7 property registry, sites, buildings, property verification, provenance, contributor workflow, and development-readiness domain",
  "exports": {
    ".": "./dist/src/index.js"
  },
  "types": "./dist/src/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "npm run build && node --test test/*.test.mjs"
  },
  "devDependencies": {
    "typescript": "^5.9.0"
  }
}
