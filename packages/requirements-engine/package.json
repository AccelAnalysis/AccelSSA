{
  "name": "@accelssa/requirements-engine",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "AccelSSA Category 4 domain model: requirements, decision criteria, assumptions, scenarios, and immutable decision-model compilation.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && node --test test/*.test.mjs"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  }
}
